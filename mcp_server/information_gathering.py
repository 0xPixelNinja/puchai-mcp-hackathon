import requests

def search_web(query: str) -> list[str]:
    """
    Searches the web for a given query and returns a list of URLs.
    """
    # This is a placeholder. We will implement this later.
    return [
        f"https://example.com/search?q={query.replace(' ', '+')}",
    ]

